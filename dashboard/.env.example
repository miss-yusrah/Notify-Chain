# Frontend environment variables for dashboard
# Visit README or Vite docs for more info about VITE_ env variables.

# Backend listener API endpoint used by the dashboard to fetch events.
VITE_EVENTS_API_URL=http://localhost:8787/api/events

# Stellar network to use: TESTNET or PUBLIC.
VITE_STELLAR_NETWORK=TESTNET
